import {Button} from 'react-bootstrap'
import {Badge } from 'react-bootstrap'

const Carrito= () => {
    return (
    
        <Button variant="primary"> Carrito <Badge bg="secondary">0</Badge>
        </Button>            

)
}

export default Carrito